{% extends 'base.html.twig' %}

{% block metatitle %}
    {{ category.title }}
{% endblock metatitle %}

{% block main %}
    <section style="background-color: {{ category.color }};">
        <h1>{{ category.title }}</h1>
    <section>
{% endblock main %}